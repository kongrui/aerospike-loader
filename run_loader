# Java Aerospike-import Script.
#
# Build with maven before running this script.
# mvn install

pushd `dirname $0` > /dev/null
SCRIPTPATH=`pwd -P`
popd > /dev/null

echo java -cp $SCRIPTPATH/target/aerospike-load-*-jar-with-dependencies.jar com.aerospike.load.AerospikeLoad $*
